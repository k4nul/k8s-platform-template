apiVersion: v1
kind: Service
metadata:
  name: adminer
  namespace: platform
  labels:
    app: adminer
spec:
  type: ClusterIP
  selector:
    app: adminer
  ports:
  - name: http
    port: 8080
    targetPort: http
    protocol: TCP
