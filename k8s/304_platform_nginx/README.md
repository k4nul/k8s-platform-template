# 304_platform_nginx

Generic reverse-proxy template based on the official NGINX image.

The default upstream points to the sample `nginx-web` service so the folder is useful out of the box even before you customize it.
