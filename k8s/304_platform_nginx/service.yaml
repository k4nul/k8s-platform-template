apiVersion: v1
kind: Service
metadata:
  name: edge-nginx
  namespace: platform
  labels:
    app: edge-nginx
spec:
  type: ClusterIP
  ports:
  - name: http
    port: 80
    targetPort: http
    protocol: TCP
  selector:
    app: edge-nginx
