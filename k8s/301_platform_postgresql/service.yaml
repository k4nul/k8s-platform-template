apiVersion: v1
kind: Service
metadata:
  name: postgresql
  namespace: platform
  labels:
    app: postgresql
spec:
  clusterIP: None
  selector:
    app: postgresql
  ports:
  - name: postgresql
    port: 5432
    targetPort: postgresql
    protocol: TCP
