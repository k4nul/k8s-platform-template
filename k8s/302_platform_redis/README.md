# 302_platform_redis

Redis cache and session-store example.

Files:

- `config.yaml`: Redis configuration
- `secret.yaml`: rendered Redis password secret
- `redis.yaml`: StatefulSet
- `service.yaml`: ClusterIP service on `6379`
