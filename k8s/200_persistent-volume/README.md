# 200_persistent-volume

Contains static persistent volume examples such as the shared NFS-backed volume.

Update these manifests when:

- you use NFS
- you want different sizes or labels
- you want to remove static PVs in favor of a dynamic storage class
